<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newstatus="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}UN-DEVOUR!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
